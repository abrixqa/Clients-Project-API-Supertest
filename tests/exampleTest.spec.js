import { expect } from 'chai'
describe('Example test', () => {
  it('Check if sum is correct', () => {
    expect(5 + 2).to.eq(7)
  })
  it('Example second test', () => {
    expect(7 * 7).to.eq(49)
  })
})
